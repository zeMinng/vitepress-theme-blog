import type MarkdownItAsync from 'markdown-it'
// import { installMarkPlugin } from '@/theme/components/mark'

export function createMarkdownPlugins(md: MarkdownItAsync) {
  // md.use(installMarkPlugin)
}
